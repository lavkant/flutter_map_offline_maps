import 'package:flutter_map_tile_caching/flutter_map_tile_caching.dart';

class DownloadService {
  late Stream<DownloadProgress> downloadProgress;
  // BehaviorSubject<DownloadProgress> get downloadProgress => _downloadProgress;

  // set downloadProgress(BehaviorSubject<DownloadProgress> stream) {
  //   _downloadProgress = stream;
  // }

  // Stream<DownloadProgress>? _downloadProgress;
  // Stream<DownloadProgress>? get downloadProgress => _downloadProgress;
  // void setDownloadProgress(
  //   Stream<DownloadProgress>? newStream, {
  //   bool notify = true,
  // }) {
  //   _downloadProgress = newStream;
  //   if (notify) ;
  // }
}
